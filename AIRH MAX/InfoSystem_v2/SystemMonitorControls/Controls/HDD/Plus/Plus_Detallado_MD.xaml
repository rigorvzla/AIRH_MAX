<Window x:Class="SystemMonitorControls.HDD.Plus.Plus_Detallado_MD"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SystemMonitorControls.HDD.Plus"
        Closed="Window_Closed"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Loaded="Window_Loaded"
        ResizeMode="NoResize"
        WindowStyle="None"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        WindowStartupLocation="CenterScreen"
        Height="450"
        Width="700"
        Background="Transparent"
        AllowsTransparency="True">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign2.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Estilo para botones de ventana modernos -->
            <Style x:Key="ModernWindowButton"
                   TargetType="Button">
                <Setter Property="Width"
                        Value="32" />
                <Setter Property="Height"
                        Value="32" />
                <Setter Property="Background"
                        Value="Transparent" />
                <Setter Property="BorderBrush"
                        Value="Transparent" />
                <Setter Property="Foreground"
                        Value="White" />
                <Setter Property="Cursor"
                        Value="Hand" />
                <Setter Property="materialDesign:RippleAssist.Feedback"
                        Value="White" />
                <Setter Property="materialDesign:RippleAssist.IsCentered"
                        Value="True" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Grid>
                                <Ellipse x:Name="BackgroundEllipse"
                                         Fill="{TemplateBinding Background}"
                                         Stroke="{TemplateBinding BorderBrush}" />
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center" />
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver"
                                         Value="True">
                                    <Setter TargetName="BackgroundEllipse"
                                            Property="Fill">
                                        <Setter.Value>
                                            <SolidColorBrush Color="White"
                                                             Opacity="0.2" />
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsPressed"
                                         Value="True">
                                    <Setter TargetName="BackgroundEllipse"
                                            Property="Fill">
                                        <Setter.Value>
                                            <SolidColorBrush Color="White"
                                                             Opacity="0.3" />
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Estilo moderno para ListViewItem HDD Plus -->
            <Style TargetType="ListViewItem"
                   x:Key="TopHDD">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate>
                            <Border Background="Transparent"
                                    CornerRadius="8"
                                    Margin="5"
                                    Padding="12">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background"
                                                Value="Transparent" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=ListViewItem}}"
                                                         Value="True">
                                                <Setter Property="Background"
                                                        Value="#20FFFFFF" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListViewItem}}"
                                                         Value="True">
                                                <Setter Property="Background"
                                                        Value="#3081d4fa" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Orientation="Horizontal"
                                                Grid.Column="0"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                        <materialDesign:PackIcon Kind="Harddisk"
                                                                 Foreground="#81d4fa"
                                                                 Width="18"
                                                                 Height="18"
                                                                 Margin="0,0,8,0" />
                                        <TextBlock Text="{Binding Etiqueta}"
                                                   Foreground="AliceBlue"
                                                   FontWeight="SemiBold"
                                                   FontSize="13" />
                                        <TextBlock Text="{Binding Unidad}"
                                                   Foreground="AliceBlue"
                                                   FontWeight="SemiBold"
                                                   FontSize="13"
                                                   Margin="3,0,0,0" />
                                    </StackPanel>

                                    <Border Grid.Column="1"
                                            Background="#20FFFFFF"
                                            CornerRadius="12"
                                            Padding="10,5"
                                            Margin="5"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Kind="ArrowUp"
                                                                     Foreground="#ff6b6b"
                                                                     Width="14"
                                                                     Height="14"
                                                                     Margin="0,0,5,0" />
                                            <TextBlock Text="{Binding Usado}"
                                                       Foreground="AliceBlue"
                                                       FontWeight="Regular"
                                                       FontSize="12" />
                                        </StackPanel>
                                    </Border>

                                    <Border Grid.Column="2"
                                            Background="#20FFFFFF"
                                            CornerRadius="12"
                                            Padding="10,5"
                                            Margin="5"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Kind="ArrowDown"
                                                                     Foreground="#51cf66"
                                                                     Width="14"
                                                                     Height="14"
                                                                     Margin="0,0,5,0" />
                                            <TextBlock Text="{Binding Libre}"
                                                       Foreground="AliceBlue"
                                                       FontWeight="Regular"
                                                       FontSize="12" />
                                        </StackPanel>
                                    </Border>

                                    <Border Grid.Column="3"
                                            Background="#20FFFFFF"
                                            CornerRadius="12"
                                            Padding="10,5"
                                            Margin="5"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Kind="Harddisk"
                                                                     Foreground="#81d4fa"
                                                                     Width="14"
                                                                     Height="14"
                                                                     Margin="0,0,5,0" />
                                            <TextBlock Text="{Binding Total}"
                                                       Foreground="AliceBlue"
                                                       FontWeight="Regular"
                                                       FontSize="12" />
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Estilo para encabezados de columna -->
            <Style x:Key="ColumnHeaderStyle"
                   TargetType="Border">
                <Setter Property="Background"
                        Value="#20FFFFFF" />
                <Setter Property="CornerRadius"
                        Value="8" />
                <Setter Property="Padding"
                        Value="12,10" />
                <Setter Property="Margin"
                        Value="5,5,5,5" />
            </Style>

            <Style x:Key="HeaderTextStyle"
                   TargetType="TextBlock">
                <Setter Property="Foreground"
                        Value="#81d4fa" />
                <Setter Property="FontWeight"
                        Value="SemiBold" />
                <Setter Property="FontSize"
                        Value="13" />
                <Setter Property="TextAlignment"
                        Value="Center" />
                <Setter Property="VerticalAlignment"
                        Value="Center" />
            </Style>

            <Style x:Key="ScrollThumbs"
                   TargetType="{x:Type Thumb}">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Thumb}">
                            <Grid x:Name="Grid">
                                <Rectangle HorizontalAlignment="Stretch"
                                           VerticalAlignment="Stretch"
                                           Width="Auto"
                                           Height="Auto"
                                           Fill="Transparent" />
                                <Border x:Name="Rectangle1"
                                        CornerRadius="5"
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Stretch"
                                        Width="Auto"
                                        Height="Auto"
                                        Background="{TemplateBinding Background}" />
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="Tag"
                                         Value="Horizontal">
                                    <Setter TargetName="Rectangle1"
                                            Property="Width"
                                            Value="Auto" />
                                    <Setter TargetName="Rectangle1"
                                            Property="Height"
                                            Value="7" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!--ScrollBars-->
            <Style x:Key="{x:Type ScrollBar}"
                   TargetType="{x:Type ScrollBar}">
                <Setter Property="Stylus.IsFlicksEnabled"
                        Value="false" />
                <Setter Property="Foreground"
                        Value="#81d4fa" />
                <Setter Property="Background"
                        Value="Transparent" />
                <Setter Property="Width"
                        Value="8" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ScrollBar}">
                            <Grid x:Name="GridRoot"
                                  Width="8"
                                  Background="{TemplateBinding Background}">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="0.00001*" />
                                </Grid.RowDefinitions>
                                <Track x:Name="PART_Track"
                                       Grid.Row="0"
                                       IsDirectionReversed="true"
                                       Focusable="false">
                                    <Track.Thumb>
                                        <Thumb x:Name="Thumb"
                                               Background="{TemplateBinding Foreground}"
                                               Style="{DynamicResource ScrollThumbs}" />
                                    </Track.Thumb>
                                    <Track.IncreaseRepeatButton>
                                        <RepeatButton x:Name="PageUp"
                                                      Command="ScrollBar.PageDownCommand"
                                                      Opacity="0"
                                                      Focusable="false" />
                                    </Track.IncreaseRepeatButton>
                                    <Track.DecreaseRepeatButton>
                                        <RepeatButton x:Name="PageDown"
                                                      Command="ScrollBar.PageUpCommand"
                                                      Opacity="0"
                                                      Focusable="false" />
                                    </Track.DecreaseRepeatButton>
                                </Track>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger SourceName="Thumb"
                                         Property="IsMouseOver"
                                         Value="true">
                                    <Setter Value="#4a9eff"
                                            TargetName="Thumb"
                                            Property="Background" />
                                </Trigger>
                                <Trigger SourceName="Thumb"
                                         Property="IsDragging"
                                         Value="true">
                                    <Setter Value="#81d4fa"
                                            TargetName="Thumb"
                                            Property="Background" />
                                </Trigger>
                                <Trigger Property="IsEnabled"
                                         Value="false">
                                    <Setter TargetName="Thumb"
                                            Property="Visibility"
                                            Value="Collapsed" />
                                </Trigger>
                                <Trigger Property="Orientation"
                                         Value="Horizontal">
                                    <Setter TargetName="GridRoot"
                                            Property="LayoutTransform">
                                        <Setter.Value>
                                            <RotateTransform Angle="-90" />
                                        </Setter.Value>
                                    </Setter>
                                    <Setter TargetName="PART_Track"
                                            Property="LayoutTransform">
                                        <Setter.Value>
                                            <RotateTransform Angle="-90" />
                                        </Setter.Value>
                                    </Setter>
                                    <Setter Property="Width"
                                            Value="Auto" />
                                    <Setter Property="Height"
                                            Value="8" />
                                    <Setter TargetName="Thumb"
                                            Property="Tag"
                                            Value="Horizontal" />
                                    <Setter TargetName="PageDown"
                                            Property="Command"
                                            Value="ScrollBar.PageLeftCommand" />
                                    <Setter TargetName="PageUp"
                                            Property="Command"
                                            Value="ScrollBar.PageRightCommand" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Border CornerRadius="15"
            Background="#FF02010F"
            Effect="{StaticResource MaterialDesignShadowDepth4}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Barra de título moderna -->
            <Grid Grid.Row="0"
                  Height="50">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal"
                            Margin="15,0,0,0"
                            VerticalAlignment="Center">
                    <materialDesign:PackIcon Kind="ChartDonut"
                                             Foreground="#81d4fa"
                                             Width="20"
                                             Height="20" />
                    <TextBlock Text=" HDD Plus - Discos"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="White"
                               Margin="8,0,0,0" />
                </StackPanel>

                <StackPanel Grid.Column="1"
                            Orientation="Horizontal"
                            Margin="0,0,15,0">
                    <Button Click="Button_Click"
                            Style="{StaticResource ModernWindowButton}"
                            ToolTip="Minimizar">
                        <materialDesign:PackIcon Kind="WindowMinimize"
                                                 Width="14"
                                                 Height="14"
                                                 Foreground="White" />
                    </Button>

                    <Button Click="Button_Click_1"
                            Style="{StaticResource ModernWindowButton}"
                            ToolTip="Cerrar"
                            Margin="5,0,0,0">
                        <materialDesign:PackIcon Kind="Close"
                                                 Width="14"
                                                 Height="14"
                                                 Foreground="White" />
                    </Button>
                </StackPanel>
            </Grid>

            <!-- Encabezados de columna modernos -->
            <Grid Grid.Row="1"
                  Margin="15,10,15,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"
                        Style="{StaticResource ColumnHeaderStyle}">
                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Center">
                        <materialDesign:PackIcon Kind="Harddisk"
                                                 Foreground="#81d4fa"
                                                 Width="16"
                                                 Height="16" />
                        <TextBlock Text=" Unidad"
                                   Style="{StaticResource HeaderTextStyle}"
                                   Margin="5,0,0,0" />
                    </StackPanel>
                </Border>

                <Border Grid.Column="1"
                        Style="{StaticResource ColumnHeaderStyle}">
                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Center">
                        <materialDesign:PackIcon Kind="ArrowUp"
                                                 Foreground="#ff6b6b"
                                                 Width="16"
                                                 Height="16" />
                        <TextBlock Text=" Usado"
                                   Style="{StaticResource HeaderTextStyle}"
                                   Margin="5,0,0,0" />
                    </StackPanel>
                </Border>

                <Border Grid.Column="2"
                        Style="{StaticResource ColumnHeaderStyle}">
                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Center">
                        <materialDesign:PackIcon Kind="ArrowDown"
                                                 Foreground="#51cf66"
                                                 Width="16"
                                                 Height="16" />
                        <TextBlock Text=" Libre"
                                   Style="{StaticResource HeaderTextStyle}"
                                   Margin="5,0,0,0" />
                    </StackPanel>
                </Border>

                <Border Grid.Column="3"
                        Style="{StaticResource ColumnHeaderStyle}">
                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Center">
                        <materialDesign:PackIcon Kind="Harddisk"
                                                 Foreground="#81d4fa"
                                                 Width="16"
                                                 Height="16" />
                        <TextBlock Text=" Total"
                                   Style="{StaticResource HeaderTextStyle}"
                                   Margin="5,0,0,0" />
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Lista de unidades -->
            <Border Grid.Row="2"
                    Background="Transparent"
                    CornerRadius="12"
                    Margin="15,0,15,15"
                    Padding="5">
                <ListView Background="Transparent"
                          BorderBrush="Transparent"
                          FontSize="12"
                          Name="ListaProcesos"
                          FontWeight="Regular"
                          Cursor="Arrow"
                          SelectionChanged="ListaProcesos_SelectionChanged"
                          ItemContainerStyle="{StaticResource TopHDD}"
                          Foreground="AliceBlue"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                          AllowDrop="False">
                </ListView>
            </Border>

            <!-- Barra de estado con información de uso -->
            <Border Grid.Row="2"
                    VerticalAlignment="Bottom"
                    Height="30"
                    Background="Transparent"
                    Margin="20,0,20,10">
                <TextBlock Text="📊 Monitoreo de espacio en disco en tiempo real"
                           Foreground="#60FFFFFF"
                           FontSize="10"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center" />
            </Border>
        </Grid>
    </Border>
</Window>