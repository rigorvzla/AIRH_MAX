<Window x:Class="SystemMonitorControls.RAM.Alarma.Alarma_Detallado_MD"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SystemMonitorControls.RAM.Alarma"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Loaded="Window_Loaded"
        ResizeMode="NoResize"
        WindowStyle="None"
        Foreground="AliceBlue"
        Background="#FF02010F"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        WindowStartupLocation="CenterScreen"
        Height="190"
        Width="200">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <materialDesign:BundledTheme BaseTheme="Dark"
                                             PrimaryColor="BlueGrey"
                                             SecondaryColor="Blue" />

                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign2.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <GroupBox Style="{StaticResource MaterialDesignGroupBox}">
        <GroupBox.Header>
            <Grid>
                <TextBlock Text="Alerta RAM"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"
                           Foreground="#FF02010F" />
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Margin="5">

                    <Button Foreground="#FF02010F"
                            Click="Button_Click"
                            Cursor="Arrow"
                            IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            Background="Transparent"
                            BorderBrush="Transparent">
                        <materialDesign:PackIcon Kind="WindowMinimize" />
                    </Button>

                    <Button Foreground="#FF02010F"
                            Background="Transparent"
                            Cursor="Arrow"
                            Click="Button_Click_1"
                            IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            BorderBrush="Transparent">
                        <materialDesign:PackIcon Kind="Close" />
                    </Button>
                </StackPanel>
            </Grid>
        </GroupBox.Header>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>

            <TextBlock Foreground="AliceBlue"
                       FontWeight="SemiBold"
                       Grid.Row="1"
                       TextAlignment="Center"><Run Text="Escribe la carga " /><LineBreak /><Run Text="para recibir la alerta " /></TextBlock>
            <TextBox x:Name="txt_CargaHDD"
                     Grid.Row="3"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     Margin="5"
                     Foreground="AliceBlue"
                     materialDesign:HintAssist.Hint="Carga %"
                     TextAlignment="Center"
                     MaxLength="4"
                     PreviewKeyDown="TxtHDD_PreviewKeyDown" />
            <StackPanel Grid.Row="4"
                        Orientation="Horizontal"
                        Margin="5">
                <ToggleButton Name="cbActivar"
                              Style="{StaticResource MaterialDesignSwitchToggleButton}"
                              Checked="CheckBox_Checked"
                              Unchecked="cbActivar_Unchecked"
                              HorizontalAlignment="Left"
                              Margin="5" />
                <TextBlock Text="Activar"
                           VerticalAlignment="Center" />
            </StackPanel>
        </Grid>
    </GroupBox>
</Window>
