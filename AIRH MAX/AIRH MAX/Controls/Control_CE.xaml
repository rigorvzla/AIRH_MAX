<UserControl x:Class="AIRH_MAX.ControlUsuario.Control_CE"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="Transparent"
             Foreground="{DynamicResource PrimaryText}"
             Loaded="Window_Loaded"
             Height="600"
             Width="700">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Theme/ScrollBarModern.xaml"></ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <StackPanel HorizontalAlignment="Stretch"
                    VerticalAlignment="Center">
            <Label Content="Mensajeria electronica"
                   FontWeight="SemiBold"
                   FontSize="14" />

            <StackPanel HorizontalAlignment="Right"
                        Orientation="Horizontal">



            </StackPanel>
        </StackPanel>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">

                <TextBox Name="TextBoxDestino"
                         Width="340"
                         Margin="5"
                         Foreground="{DynamicResource PrimaryText}"
                         materialDesign:HintAssist.Hint="Destino"
                         FontWeight="Medium"
                         Background="Transparent"
                         TextWrapping="Wrap"
                         SelectionBrush="{DynamicResource SecondaryText}" />
            </StackPanel>
            <StackPanel Grid.Column="1"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">

                <TextBox Name="txtAsunto"
                         Width="350"
                         Margin="5"
                         Foreground="{DynamicResource PrimaryText}"
                         materialDesign:HintAssist.Hint="Asunto"
                         FontWeight="Medium"
                         Background="Transparent"
                         TextWrapping="Wrap"
                         SelectionBrush="{DynamicResource SecondaryText}" />
            </StackPanel>
        </Grid>
        <Grid Grid.Row="2">
            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                <TextBox Name="txtContenido"
                         AcceptsReturn="True"
                         VerticalContentAlignment="Top"
                         Width="680"
                         Height="490"
                         Margin="5,0,5,0"
                         materialDesign:HintAssist.Hint="Mensaje"
                         Foreground="{DynamicResource PrimaryText}"
                         FontWeight="Medium"
                         Background="Transparent"
                         TextWrapping="Wrap"
                         SelectionBrush="{DynamicResource SecondaryText}"
               />
            </StackPanel>
        </Grid>
        <Grid Grid.Row="3">
         
            <Grid Grid.Column="0"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                <materialDesign:PackIcon Kind="EmailBox"
                                         Width="23"
                                         Height="25"
                                         HorizontalAlignment="Center"
                                         VerticalAlignment="Center"
                                         Foreground="{DynamicResource PrimaryText}" />
                <RadioButton Click="btnEnviar_Click"
                             Foreground="{DynamicResource PrimaryText}"
                             ToolTip="Enviar Mensaje"
                             Style="{StaticResource MaterialDesignOutlinedDarkButton }">

                </RadioButton>

            </Grid>
        </Grid>
    </Grid>
</UserControl>
